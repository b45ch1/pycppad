#
# NOTE! Don't add files that are generated in specific
# subdirectories here. Add them in the ".gitignore" file
# in that subdirectory instead.
#
# General purpose rules for python packages
#
.*
*.*~
*.a
*.dylib
*.py~
*.pyc
*.so
build
#
# Special temporary extension (created by build.sh)
*.log
# Special temporary files (created by build.sh)
dist
MANIFEST
MANIFEST.in
doc
external

# Special temporary file (created by commit.sh)
commit.old

# Automatically generated (by build.sh) file that is in pycppad distribution
test_example.py

# exceptions
!.gitignore
